# Optional frontends for GetME. The backend is API-first; these are interchangeable clients.
#
# web-agent/   — guest + organizer UI served by the laptop GetME process
# web-studio/  — signup + studio UI served by the cloud relay
